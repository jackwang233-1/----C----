#include <stdio.h>

int main()
{
        printf("input the size of the first matrix :");
        int m, s, p, n;
        scanf("%d * %d", &m, &s);

        int ma1[m][s];

        printf("input the size of the first matrix :");
        scanf("%d * %d", &s, &n);

        printf("m = %d, s = %d, n = %d", m, p, n);

        if (p != s)
        {
            printf("These two matrix can't be multiplied.")

        }
        else
        {

        int ma2[s][n];

        printf("input the value of the 1st matrix:\n");











        }
        return 0;
}
